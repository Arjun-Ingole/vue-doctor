{
  "name": "vue-doctor-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter vue-doctor dev",
    "build": "pnpm --filter vue-doctor build",
    "test": "pnpm --filter vue-doctor test",
    "lint": "oxlint .",
    "format": "oxfmt ."
  },
  "devDependencies": {
    "oxlint": "^1.0.0",
    "typescript": ">=5.0.4 <7"
  },
  "packageManager": "pnpm@10.0.0"
}
